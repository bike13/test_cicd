{
  "name": "hello-world-app",
  "version": "1.0.0",
  "description": "一个简单的Hello World应用，用于Jenkins CI/CD测试",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required for static files\"",
    "deploy": "echo \"Deployment handled by Jenkins\""
  },
  "keywords": [
    "hello-world",
    "jenkins",
    "cicd",
    "frontend",
    "test"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
